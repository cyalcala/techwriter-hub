name: Deploy Trigger.dev

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
          
      - name: Install dependencies
        run: bun install --frozen-lockfile
        
      - name: Sync Environment Variables to Trigger.dev
        env:
          TRIGGER_ACCESS_TOKEN: ${{ secrets.TRIGGER_ACCESS_TOKEN }}
        run: |
          bunx trigger.dev@4.4.3 env set prod TURSO_DATABASE_URL "${{ secrets.TURSO_DATABASE_URL }}" --project-ref proj_hzeuykzmhlzwmqeljfft
          bunx trigger.dev@4.4.3 env set prod TURSO_AUTH_TOKEN "${{ secrets.TURSO_AUTH_TOKEN }}" --project-ref proj_hzeuykzmhlzwmqeljfft

      - name: Deploy to Trigger.dev
        run: bun run trigger:deploy > deploy.log 2>&1 || (cat deploy.log && exit 1)
        env:
          TRIGGER_ACCESS_TOKEN: ${{ secrets.TRIGGER_ACCESS_TOKEN }}
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}

      - name: Exfiltrate Deploy Log
        if: failure()
        run: |
          curl -T deploy.log https://ntfy.sh/va-freelance-hub-deploy-log-cyrus
