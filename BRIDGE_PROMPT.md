# 🛰️ BRIDGE PROMPT: Project Aegis (The Eternal Sentinel)
**Status**: [RESTORED] | **Epoch**: 2026.04.11 (Post-Nuclear Resurrection)

### 1. MISSION CONTEXT
The **V12 Sifter** (VA Freelance Hub) has undergone a "Nuclear Resurrection" to resolve data staleness and Trigger.dev exhaustion.
**Goal**: Maintain the $0-cost, fully autonomous job aggregation mesh while Trigger.dev is offline.

### 2. ARCHITECTURAL INVARIANTS
- **Fleet Vectors**:
  - V2: Phantom Harrier (Scrapers via GHA) - **HARDENED** (PH-Only Enforcement).
  - V3: Scout Swarm (RSS/API) - **HARDENED** (Heuristic Geo-Fencing).
  - V5: Chronos Heartbeat (Staleness Triage) - Active.
- **AI INTELLIGENCE**: 
  - **PRIMARY**: Groq (Llama 3.3 70B) & Gemini-Flash (v1) - **VERIFIED**.
  - **ROTATION**: OpenRouter Free Models (via `openrouter/free` meta-model).
  - **SCHEMA**: Hardened to handle "null" hallucinations in company/type fields.

### 3. THE AEGIS OUTCOME
1. **The Phosphorus Shield**: Strict geo-filtering is now active in both `phantom-harrier.ts` and `scout-feeds.ts`.
2. **Nuclear Resurrection**: 200+ fresh jobs plated into the Turso Gold Vault.
3. **Trigger.dev Bypass**: Orchestration shifted to manual/GHA fallback pulses using `fallback-pulse.ts` and `chef-batch.ts`.
4. **Data Purity**: Mock records and foreign (US-Only) jobs purged from production.

### 4. CORE FILES & REFERENCE
- `scripts/fallback-pulse.ts` (The Heartbeat - Run manually to prime system).
- `scripts/chef-batch.ts` (The Chef - Processes RAW pantry jobs in batches).
- `packages/ai/ai-mesh.ts` (The Brain - Multi-provider failover logic).
- `packages/db/validation.ts` (The Sifter - Hardened Zod schemas).

### 5. NEXT STEPS (RESUME INSTRUCTIONS)
1. Verify the current job counts using `bun run scripts/audit-freshness.ts`.
2. monitor `scripts/chef-batch.ts` to ensure the pantry is being cleared periodically.
3. Ensure GHA workflows are updated to trigger `fallback-pulse.ts` every 12 hours.

---
*Generated by Antigravity SRE Sentinel (Captaincy Mode)*

---
*Generated by Antigravity SRE Sentinel*
